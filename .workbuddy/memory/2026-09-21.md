# 2026-09-21

## Wayfinder 续跑：D2（#6）grilling 第一轮 —— 票面前提被推翻

用户以「hand off to /wayfinder」把 effort 交回。按 map 的顺序取第一张 frontier 票 = **D2「小程序 × 中控屏职责边界：取件码怎么流转」（#6）**，session 只解这一票。
（#6 的 assignee 已是我，无 comments —— 视为上一 session 的弃置 claim，按 map 的 frontier 列表照常取用。）

### 关键事实（票面隐含假设不成立，须先纠正）

D2 票面把「小程序出二维码给屏扫 vs 屏上敲 6 位码」当成 A/B，隐含「屏上敲码 → 开门」。**代码里不成立**：

- 开门只能由服务端下发的 `OPEN_CELL` 指令驱动，指令来源**只有三个**：
  1. `13.7 POST /orders` 下单 → `USER_DROP`
  2. `13.11 POST /orders/:id/pay` 支付 → `USER_PICKUP`（**支付即开门**）
  3. 管理端手动开箱 → `ADMIN_FORCE`
- **屏端 `pickup-verify` 不产生任何指令** —— 只回摘要 + `paid` + `hint`（`PickupVerifyServiceImpl.java:50-71`）。
  屏端后端原文 hint：`paid:false` →「请在小程序完成支付后取件」；`paid:true` →「请在小程序点击取件开门」。
- ⇒ **取件码没有开门力**；真正开门发生在**小程序的 13.11**（幂等：已付清再调不重复扣费、只重发指令，`openCommandIssued` 告知本次是否下发）。
  - `AppPickupPaymentFlowTest`：`pay_freezesFees_recordsIncomeAndIssuesPickupCommand`、`pay_repeated_doesNotDoubleCharge_butReissuesPickupCommand`、`pay_notYetStored_returns3002`、`pay_afterPickup_returns3002`。
  - `需求分析文档.md:157` 亦写明「小程序入口：进行中订单 →『取件』→ …→ 生成开门指令（`USER_PICKUP`）」。
- 屏侧唯一取件入口是键盘键入 6 位码；屏端**无入站扫码能力**（R2 已证）⇒「出码给屏扫」需屏侧改造，Out of scope。

### 由此推出的边界（待 grilling 裁决）

- 屏 = 只读查询台（查状态 + 应付参考），小程序 = 唯一开门发起方。
- C 端**必须有一个「开门取件」动作**，实现 = 幂等 `13.11`；未付清先付、已付清重发指令。
- 「把码发给别人代取」在当前架构下**不可能**（开门只认本人 token，越权 `8003`）。

## 第一轮 grilling 裁决（用户「全部按推荐」）

- **Q1** 屏 = 只读查询台；小程序 = 唯一开门发起方。C 端必须有「开门取件」动作，实现 = 幂等 `13.11`。跨端不传任何上下文（承 D1）。
- **Q2** 取件码**只在订单详情页**常驻可见（不隐藏、不设倒计时）；列表行内不铺码，只给进入详情的入口（列表结构归 D3）。
- **Q3** 只解释「未存入期码尚未生成」（详情页固定取件区块里写明「关门后生成」，占位非空白）；终局后不解释，交给状态。
- **Q4** 「把码发给别人代取」判死并写进「刻意不做」+ map 的 Out of scope；码的展示不做防截屏；任何界面不得暗示「凭码取件」。

### `13.11` 的取值细节（第二轮用到的硬事实）

- 允许时机 = 已存入未取件（`IN_PROGRESS|TIMEOUT` ∧ `startAt≠null` ∧ `endAt==null`）；**未存入 → `3002`**（存件期的开门由 `13.7` 下单自动下发的 `USER_DROP` 承担）；已取件/终局 → `3002`。
- `TIMEOUT` 单：支付时刻现算**差额**（累计应收 − 已付），已付清不扣费。
- 幂等：已支付未取件 → 不重复扣费 + **重发 `OPEN_CELL(USER_PICKUP)`** + 返回首次支付信息。
- ⚠️ **`paid` 与 `openCommandIssued` 一期都是写死的 `true`**（`AppOrderServiceImpl.java:199`）⇒ `openCommandIssued` 不是「指令下发了」的证据，C 端**不许**据此渲染「门已开」；唯一证据是 `DOOR_CLOSED` 驱动的状态推进。这条是交给 P1 的输入。

## 第二、三轮 grilling 裁决（用户「全部按推荐」×2）

- **Q5** 详情页取件区块**预先讲清流程**（消解「输码即开门」的预期落差）；屏上 hint 只作事后兜底。
- **Q6** **一个按钮，按状态换文案与语义**：支付并开门 / 取件开门 / 补缴并开门；**重试入口就是同一个按钮**；点完不许渲染「门已开」。
- **Q7** 动作统一叫「**取件开门**」，与屏端 hint 逐字对齐；落 `CONTEXT.md`。
- **Q8** 九态对齐表收成（对齐是**语义级**的 —— C 端看不到屏的显示态）。
- **Q9** 点完只说「已发出开门指令…」+ 停等待态。
- **Q10** 派生判定：**能不能取件 = `pickupCode !== null`**（与 `13.11` 准入、`14.6` 校验同源）；**付没付清 = `payAmount !== null`**（全免单是 0 元 `SUCCESS` 支付单，用 `> 0` 会把全免单卡在待支付）。
- **Q11** **按钮不承载金额**（`estimatedAmount` 只是 5 分钟粒度参考快照，契约禁渲染为锁定价）；金额单独一行带「参考」二字。
- **Q12** 取件不可达的兜底 = 详情页 + 「我的」页写明联系客服；人工出口是管理端 `ADMIN_FORCE`（不触发自动结算）。「联系客服」第一次成为功能性出口。

## D2 收口执行

- 产出 `docs/spec/02-c-end-device-boundary.md`（13 节：前提事实 / 分工 / 码的定位 / 展示口径 / 看不到码 / 动作 / 派生判定 / 点完之后 / 九态对齐 / 兜底 / 移交说明 / 刻意不做 / 留给其它 ticket）。
- 新建 repo root **`CONTEXT.md`**（single-context 首个 glossary，7 个术语）。
- **屏侧无需改动** —— 只有两条登记项：① 屏上 hint 是后端写死字符串，改名需人工同步；② 屏端键盘提交按钮文案是「开门」但只做校验（Out of scope，不改屏）。
- Resolve：评论 answer → close #6 → map #1 的 Decisions so far 追加 pointer、Not yet specified 清掉已 graduate 的 fog、Tickets 的 frontier 行更新；向 P1 #12 留前置输入评论。

## 19:17 续跑（第二 session）：claim D3（#7）

用户再次以「hand off to /wayfinder」交回。取 map 顺序第一张 frontier 票 = **D3「订单首页如何表达未存入 / 使用中 / 异常」（#7）**，已 `gh issue edit 7 --add-assignee @me` claim。本轮只解这一票。

（Frontier 现状：D3 #7 / D4 #8 / D5 #9 / D6 #10 / D7 #11 / P1 #12 / T2 #14 均可开；T1 #13 被 D3 阻塞。全部 unassigned；#7 的 `blocked_by` 已归 0。）

### D3 的事实底稿（自己查的，未派 subagent）

- **列表契约 §13.8**：`GET /api/app/v1/orders?status=&page=&pageSize=`。`status` 是**单值**枚举（`PENDING_PAYMENT|IN_PROGRESS|COMPLETED|TIMEOUT|FORCE_ENDED|REFUNDED|CANCELLED`），**没有多值 / 分组参数**。
- **列表项字段**（`AppOrderListItemVO.java`）：`id / orderNo / lockerId / lockerNo / siteName / cellNo / cellType / startAt / endAt / status / payAmount / estimatedAmount / pickupCode`。**没有 `createdAt`**，**没有 `openTimeoutMinutes`**。
- **服务端排序**（`AppOrderServiceImpl.list()`）：`.orderByDesc(Order::getId)` —— 即下单倒序，客户端不排序即可。
- **`openTimeoutMinutes` 只出现在 13.7 建单响应**（`CreateOrderVO`），列表与详情都没有 ⇒ 冷启动后无法知道未存入单的时限基准。
- **真实截止时刻是两段式**（`NotYetStoredDeadline.stageOf`）：第一程 = `createdAt + openTimeoutMinutes`；**开门指令未回执 `DONE`** → 取消；**已回执** → 延长一程再看，超期标记「开门未闭」。指令是否 `DONE`（`openAcked`）**C 端完全看不到** ⇒ 任何客户端倒计时都可能说谎。
- 「开门未闭」判定 `awaitingReview` = `openedNotClosedAt != null && status == IN_PROGRESS`，而 `openedNotClosed` **不在 C 端 §13.8/§13.9 契约内**（R3 唯一真缺口）⇒ C 端只能漏报，`IN_PROGRESS + startAt=null` 长期不动与「已延长一程」不可区分。
- **TIMEOUT 跨两类**：`endAt == null` 是在途（待补缴/待取件），`endAt != null` 是已终局 ⇒ **按 status 分组会把同一枚枚举劈成两半**，「进行中 / 已完成」两级模型与枚举错位。
- 状态机（需求 §4.5）：`IN_PROGRESS`（含未存入 `startAt=null` 与使用中）→ `CANCELLED` / `COMPLETED` / `TIMEOUT` / `FORCE_ENDED` / `REFUNDED`；`PENDING_PAYMENT` 一期不启用。
- 屏端色调层事实源：`vue/smart-locker-device/.scratch/device-simulator/spec.md` V9/V25（语义色只做背景/边框/圆点/色条，文字走 ink；琥珀 ink `#976a27`、红 ink `#c05454`、绿用 `$color-primary-active #008655`）。
- 分页：`PageSupport.DEFAULT_PAGE_SIZE = 20`、上限 100、**越界钳制到末页** ⇒ 上拉加载的终止条件只能是「已加载条数 ≥ total」。
- 行内版式的空间维度：device 侧门面只有**物理门号 `01`–`15`**（device spec V3），C 端只有 `cellNo`（`M05`），映射表不在 C 端契约 ⇒ 登记项，不改两边。

## D3 收口（#7 resolved）

两轮 grilling：Q1–Q7（骨架）、Q8–Q13（版式与派生），用户两次「全部按推荐」。

- 产物 **`docs/spec/03-order-list.md`**（12 节：前提事实 / 单流分组 / 排序与在途上浮 / 9 个显示态 / 行内版式 / 三列映射草案 / 刷新与分页 / 异常浮现 / 空态 / 登记项 / 刻意不做 / 留给其它 ticket）。
- **13 条裁决**要点：单流不分组（不做 tab/分段/筛选）；**第一页内在途上浮、跨页不置顶**；异常混排不做「待处理」带；**不做未存入倒计时**；一个空态；`onShow`+下拉刷新且**重置回第一页**、列表不轮询；取消只在详情页；11 枚举 → **9 显示态**（「已取件」合并 `COMPLETED` 与补缴取件后的 `TIMEOUT`，「已超时」刻意不二分已补缴）；行内 3px 色条 + 站点名 + `柜机码 · 格口号` + 时间 + 状态标签，**行内不给金额、不给动作、不铺取件码**；语义档 警示/正常/危险/中性（文字走 ink）；`pageSize=20` + 上拉加载、终止靠 `total`。
- **就地补正 D2**（`docs/spec/02-c-end-device-boundary.md`）：§6 表格「已存入、已付清」限定为**仅 `IN_PROGRESS`**，`TIMEOUT` 行改「无论已付多少」都走「补缴并开门」；§7 派生判定改为 **`payAmount !== null ∧ status === IN_PROGRESS`**。根因：已支付未关门的单会被 5min 任务转 `TIMEOUT`，而 `TIMEOUT` 应收**继续累积**（`BillingServiceImpl.estimatedAmount` 只在 `IN_PROGRESS` 收敛到定格值）⇒ 旧口径会让按钮文案说谎。备选（`estimatedAmount > payAmount` 反推欠费）否决（5 分钟**向上**量化会误报）。差额细节仍归 D6 #10。
- 登记项两条：物理门号 vs `cellNo` 不同源；「开门未闭」C 端无字段 ⇒ **漏报**（保守方向）+ 联调挂账。
- Resolve 动作：评论 answer（草稿 `.scratch/smart-locker-app/comment-D3.md`）→ close #7 → map #1 追加 pointer、清 `Not yet specified`、更新 frontier；**T1 #13 随之解锁**。
- ⚠️ **踩坑（本次实测，已收入 skill `github-issues-headless-linux` 第 8 步）**：收口当刻 **GitHub API 全线 `Bad Gateway`** —— `gh issue comment`/`view`（走 **GraphQL**）与 `gh api`（走 **REST**）同时失败，`curl https://api.github.com` 报 `CONNECT tunnel failed, response 502`；而 `https://www.baidu.com` 与 **`git` over SSH（22 端口）都正常** ⇒ 沙箱出网代理对 github.com 的隧道故障，**不是**令牌/仓库问题。
  - 连接器**不是**出路：公共仓库下 `mcp__github__add_issue_comment` 仍 `403 Resource not accessible by integration`（再次证实）。
  - 正确降级顺序：**本地产物照做 → `git commit` + SSH `push` → 有界重试探测 API → 恢复后一次成形补写**（评论 → close → 改 map，顺序不可反）。本次第 1 次重试即恢复，写操作全部补齐。

---

## D4 开工（#8 claimed，2026-09-21 20:05）

`/wayfinder` 第二次进场（用户「hand off to @skill:wayfinder」）。加载 map #1 → frontier = D4 #8 / D5 #9 / D6 #10 / D7 #11 / P1 #12 / T1 #13 / T2 #14（**全部解锁**，D3 #7 于 19:55 关闭后连 T1 也开了）。按 map 顺序取第一张 → **`gh issue edit 8 --add-assignee @me`**（claim 完成）。

### 开工前核实的事实（D4 用，逐条对后端原文行号）

- **`5004` 拦截面 = 恰好 3 个动作**：`13.7` 建单 / `13.12` 创建预约 / `13.15` 开始使用（API :64、:1295「用户端下单/创建预约/开始使用」；需求 :134）。服务端 `message` 原文 = 「**黑名单用户禁止该操作**」。
- ⚠️ **`13.11` 支付不被 `5004` 拦**（直接核 :1057-1084 规则 1–4，只列 `3002/8003/3001/6001`）⇒ **拉黑只封「开新动作」，不封存量订单收尾**。`13.10` 取消、`13.4`/`13.8`/`13.9`/`13.13`/`13.14`/`13.16` 也都不拦。
- **校验序**：`13.7` = `5004 → 4004 → 8002`；`13.12` = `5004 → 4004 → 8002 → 8006 → 8007`；`13.15` = **`5004` 判在 `8003`（归属）之前**（:1129）⇒ 黑名单用户对任何预约 id 都会先撞 5004。
- **C 端 `blacklistRecord` 只有 `{reason, createdAt, auto}`**（:956/:959）；管理端 9.4 才有 `releaseCondition`（:581）⇒ **C 端说不出解除条件 / 剩余天数**。
- **`score.blacklistThreshold`(60) / `autoRemoveDays`(30) / `noShowDeduct`(10) / `timeoutDeduct`(5) 全是可配置参数**（需求 :305-316、管理端参数端点 :864）而 **C 端 16 端点里没有任何参数查询**（R1 §5 + 直接核 §13 全文）⇒ **这些数字 C 端拿不到**。唯一例外：`openTimeoutMinutes` 由 `13.7` 响应回显。
- 自动拉黑的 `reason` 服务端原文**含阈值数字**（「积分低于黑名单阈值（60）」，:626/:633）⇒ 直出会泄露内部参数。
- 扣分触发点（需求 §4.8）：订单超时 `TIMEOUT` −5；预约爽约 `NOSHOW` −10；恶意占用 −10（一期人工）；管理端手动 ±。初始 = `score.max` = 100。
- **R1 的一处错误待更正**：R1 把 `13.16 score-logs` 标成 `[未明确]`（「文档未贴出字段形状」），但形状**就在** API 文档 §9.3 的 JSON 块里（:588-596）：`{id, delta, reason, type: AUTO|MANUAL, operatorName, createdAt}` + `total`。⇒ 应像 13.6 那次一样给 findings 分支打更正（待 D4 收口时一并 commit）。
- 01 spec 已预留接口位：`:178`「黑名单 / 低信用分的可见性矩阵与禁用清单 | D4 #8」、`:129`「本页骨架须同时容纳 D5/D4 两方结论」、`:128` 待复核「联系客服的载体（`open-type="contact"` vs 电话/微信号）」。03 spec `:170` 同样留位。

### 待裁决（Round 1 已发，8 问）

承载模型 / 被拦动作表现 + 存量订单收尾权 / `5004` 是否特判（禁走通用 `message` 直出）/ `reason`+`auto` 呈现口径与屏端「业务拒绝」的区分 / 申诉路径 + 客服载体 / 信用分入口与明细呈现 / **扣分预警强度** / **「后端拿不到的配置值」的统一表述口径（跨 D4/D5/D6/T1）** / 解除后状态。

⚠️ 第 8 问是本次新浮现的跨票缺口，倾向「只讲机制不讲数字」的硬规则 + 登记挂账，不新开票（避免收窄 frontier）。

### D4 Round 1 —— 用户答「全部按推荐」（2026-09-21 20:19）

八条全部采纳：

1. **承载模型 = 只在「我的」页常驻 + 动作点就地解释**（不挂全局横幅）。
2. **三个被拦动作（建单 / 创建预约 / 开始使用）一律「保持可见、可点、点击后解释」**，不置灰不隐藏；**强制例外**：存量已存入订单的「取件开门」保持正常可用 + 附近写明「受限期间仍可取回已存入物品」。
3. **`5004` 特判**，不许走通用「按 `message` 直出」通道（其余错误码不变）。服务端 `message` 是「黑名单用户禁止该操作」—— 不裸码但也不解释出路。
4. `reason` **按 `auto` 分叉**：`auto=true` 走映射文案、不直出阈值数字；`auto=false` 直出管理端原文；两者附 `createdAt`。文案上把「账号被限制」与屏端「**业务拒绝**」显式分开。
5. 申诉 → **说机制不说天数**（自动限制「到期自动解除，以页面状态为准」；人工限制 → 联系客服）。客服载体定 **`open-type="contact"`，主体未配时退化为展示电话**（⇒ 须就地清掉 `01` §5 的那条「待复核」）。「C 端缺 `releaseCondition`」登记为契约缺口。
6. 「我的」页给**分值 + 一行状态语义**；明细**独立页**、`pageSize=20` + 上拉；`operatorName` 不展示（`AUTO`→「系统」/`MANUAL`→「管理员」）；扣分方向用**警示/危险色**（分数下降是坏事，别套股票红涨绿跌）。⇒ 须就地回填 `01` §5 的「积分明细入口（呈现形态由 D4 #8 收口）」。
7. **硬规则：C 端不得展示任何无法从 16 端点回显的配置数字**，文案一律「讲机制、不讲数字」；唯一例外 `13.7` 回显的 `openTimeoutMinutes`。落本票 = **只做事后告知，不做进度条/「还差 X 分」**。
8. **不留痕**、对称回落（解除后 `status`→`NORMAL`、`blacklistRecord`→`null`），不做本地 `status` 快照（承 `01` §3.2「不做本地业务数据缓存」）。

**Round 2 已发（Q9–Q14）**：「我的」页受限卡形态 / **已知受限时「事后解释 vs 事前阻断」**（冷启动 `13.1` 响应即带 `user.status`，所以 C 端从第一屏就知情 —— 但推荐仍走事后，理由是**拦截权只能属于服务端**，本地快照会造出双源真相）/ 订单侧受限浮现（含存量单的**取消 `13.10` 也不被 5004 拦**，须与取件一并确认「收尾动作全可用」）/ 拿 `5004` 时**不猜**更具体的原因（它遮蔽 `4004/8002/8003/6201/6202`）/ Q7 硬规则的精确判据（「服务端回显过的值」而非「非配置项」）/ **本票范围是否兜 `4004`+`8002`**（与 5004 同在一张校验序表，且经查 `03` §12 与 `01` §7 均未认领 ⇒ 悬空）。

### D4 Round 2 —— 再次「全部按推荐」（2026-09-21 20:37）

1. 「我的」页 = **积分行常驻 + 受限时插受限卡**（解释 + `createdAt` + 三项不可用动作清单 + 「已存入订单不受影响，仍可正常取回」+ 客服）；`auto=false` 的自由文本 reason **折叠 3 行 + 可展开**。
2. **事后解释，不做事前阻断**；柜机页也不挂预防提示。
3. **列表行不加受限标记**；详情页取件区块补一行「账号受限不影响取回本单」（与 `02` §10 客服出口并置，不新增第二个出口）；**存量单收尾动作全集 = 取件开门 + 取消，都保持可用、都额外不解释**。
4. 拿 `5004` **只陈述受限、不猜**更具体原因（归因权只在服务端；本地算不出窗口与余位）。
5. Q7 判据改为 **「服务端是否在响应里回显过」**：可展示 `openTimeoutMinutes`/`estimatedAmount`/`payAmount`/`planStartAt`/`planEndAt`/`total`/`score`/`delta`；禁展示 `blacklistThreshold`/`autoRemoveDays`/`holdMinutes`/`noShowMinutes`/`maxHours`/`timeoutDeduct`/`noShowDeduct`，并禁把 `openTimeoutMinutes` 挪到别的场景复用。
6. **本票兜住 `4004` / `8002`**（只写失败表现，不写恢复机制）。

**Round 3 已发**：可见性矩阵（界面 × `NORMAL`/`BLACKLISTED`）核对 + 4 个残余决策 ——
`Q15` 「哪些行为会扣分」的规则说明载体（推荐：信用分明细页顶部折叠段，不新增 `01` §5 静态出口项）；
`Q16` **积分明细行的 `reason` 也走同一套分叉**（新证据：需求 :231「自动拉黑在积分明细留痕」+ API :633 的自动文案自带阈值 ⇒ 明细页本来也会泄露 60；判据统一成 `AUTO`/`auto` = 系统生成→映射脱敏，人工填写→直出）；
`Q17` **收回 Q5 的「退化」说法** —— `open-type="contact"` 是否可用**在运行时探测不到**（配置在微信后台，未配时点击无反应）⇒ 只做按钮、不做运行时切换、不做双出口，「后台须配客服」登记为**上线前置项**；
`Q18` **术语落库**（domain-modeling）：`CONTEXT.md` 目前 7 条词、**无黑名单/信用分相关词**。候选：用户可见词用 **「受限」**（Avoid 封号/被拉黑）、**「信用分」vs「积分」**（推荐 canonical = 信用分，理由：它是扣减型行为约束、不是可兑换的消费积分；代价：须就地改 `01` §5 的「积分」区块名）、屏端「业务拒绝」**不入**本仓库 CONTEXT（属 device 侧）。

同时确认本票边界：只兜**准入类** `5004`/`4004`/`8002`；单笔业务校验失败（`3002`/`3001`/`6201`/`6202`/`8003`/`6001`）留在各自动作所在票。


