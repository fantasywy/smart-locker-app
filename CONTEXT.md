# smart-locker-app

智能柜的**小程序 C 端**。它不拥有柜机，也不拥有账本 —— 柜机由中控屏那一侧驱动，订单与金额由后端定义，小程序只负责「把用户和柜机接上」。本文件只是术语表，实现口径见 `docs/spec/`。

## Language

### 柜机一侧

**柜机码**：
印在柜机上的编号（如 `WD-01`），是小程序找到某台柜机的唯一途径。用户扫的就是它。
_Avoid_: 设备码、柜机编号、二维码内容

**中控屏**：
柜机上那块公共触摸屏。用户在上面敲取件码、看订单摘要。
_Avoid_: 柜机屏、设备端、终端

**开门指令**：
服务端下发给柜机的开门命令，是**开门这件事的唯一驱动**。小程序开不了门，它只能请服务端生成一条指令。
_Avoid_: 开门请求、开箱命令

### 取件码

**取件码**：
6 位数字，同一柜机内唯一、历史不复用，用户**存入关门后**才生成。
它是**查询凭据，不是取件凭据** —— 在中控屏上敲入它能查出「哪一单、欠多少、付没付」，但它**打不开门**。
_Avoid_: 取件凭证、开箱码、取件密码

**取件开门**：
C 端唯一的开门动作。它把「清账」和「开门」合成同一个动作 —— 没清账就先清账，清过了就直接开门并重发指令。
_Avoid_: 取件、开箱、一键开门、支付（支付只是它未清账分支里的实现细节）

**参考金额**：
支付前展示给用户的费用预估，**5 分钟粒度向上量化的快照**，**永远带着「参考」二字**。
它不是锁定价 —— 实扣以支付时刻服务端现算为准，**在任何界面上都不得渲染为应付金额**。
与「实付」必须分开讲：参考金额随用时增长（`TIMEOUT` 单上仍在增长），实付是已定格的历史。
_Avoid_: 应付金额、锁定价、预估价（「预估价」丢掉了「不作数」这层意思）

**实付**：
订单上**已定格**的累计实收（后端字段 `payAmount`）。
⚠️ 它是**累计**的，不是某一笔 —— 补缴单上「实付」会大于任何单次支付额。
所以它在 `TIMEOUT` 单上**只能叫「已付」，不能叫「已付清」**：应收仍在累积，说「清」就是谎。
与「本次支付额」区分：后者只是某一笔支付的金额，**只配出现在支付那一刻的即时反馈里**，不作常驻展示（页面刷新就没了）。
_Avoid_: 已付清、支付金额（两者都不区分「累计」与「单笔」）

**补缴**：
`TIMEOUT` 单取件时的付费动作 —— 收取「现算应收 − 累计已付」的**差额**（下限 0）。
它不是第二次「支付」：用户面对的仍是**同一个按钮**（只是换了文案），调的是**同一个端点**。
差额由服务端算，**客户端不得自算** —— 参考金额是向上量化的快照，反推欠费在边界上必然误报。
_Avoid_: 二次支付、续费、欠费补交（都要么暗示新交易、要么暗示客户端能算出欠多少）

### 订单两段

**存入**：
订单的前半段 —— 下单开门、放东西、关门。**关门才是落定点**：关门事件之前订单没有计时起点，也就没有费用。
_Avoid_: 寄件、寄存

**取件**：
订单的后半段 —— 清账、开门、取走、关门结算。
_Avoid_: 领取、签收

**关门落定**：
**柜门关闭事件驱动订单推进**这件事本身 —— 它是全系统里「物理事实 → 业务状态」的**唯一接口**，也是**唯一能证明门真的开过的证据**。
⚠️ 它落在两条路段的语义**相反**：**存入**关门 = 落定计时起点、订单**才刚开始**；**取件**关门 = 结算落账、订单**就此结束**。
_Avoid_: 门已开、已开门、开门成功（都是「发出指令」层面的说法，证明不了门开过）

### 等待关门

**等待关门**：
用户按下开门动作之后、**关门落定**之前的那一段 —— 界面进入的等待状态。
它**只能说「已发出开门指令」**，因为此刻手里的真相只有「我们请服务端发了一条指令」：指令有没有送达、门有没有真的打开、用户关没关门，**C 端一概看不到**。
⚠️ 它与**存入/取件**是**正交**的：存入和取件都会经过它，只是关门之后的落点相反。
_Avoid_: 处理中、开门中、取件中（「处理中」什么都没说，且会与请求进行态撞名）

**已发出开门指令**：
等待关门期间**唯一可以说出口的事实** —— 陈述的是「我们刚做过的那个动作」，不是任何结果。
它与「门已开」严格区分：后者断言了门的状态，而**没有任何字段能证明它**。这条区分是硬规则，不是措辞偏好。
_Avoid_: 门已开、开门成功、取件成功

### 预约

**预约**：
为**未来某时段**锁住一个格口的动作，创建即把格口置 `RESERVED`。它**不产生订单**，直到用户「开始使用」才转成存件订单。
与「存件」的区别：存件是**此刻**就要用柜（走扫码），预约是锁**未来**的位。所以 `planStartAt` 早于当前时刻的预约会被拒（提示走扫码存件）。
_Avoid_: 预订、预定、占位

**使用窗口**：
预约可被「开始使用」的时间区间 —— `[planStartAt − 提前宽限, planStartAt + 爽约时限)`，**半开**。左界之前开始不了，右界一到就算爽约。
⚠️ 它是**服务端的判定区间**，两个端点（15 / 30 分钟）**都不回显给 C 端**，所以界面**只能说「时段前后一小段时间内都可以开始」，不能说具体分钟数**。
_Avoid_: 有效期、可用时间、取件窗口

**爽约**：
到了使用窗口右界仍未「开始使用」，预约被判 `NOSHOW` —— **这是用户的责任，会扣信用分**。
与「失效」必须分开讲：爽约是用户没去，失效是柜机坏了。
_Avoid_: 违约、逾期、过期

**失效**：
预约因**柜机/格口故障**被系统置 `EXPIRED` —— **不是用户的责任，不扣分**。
用户会问「我的预约怎么没了」，所以它必须与「爽约」在文案上一眼分开（归因纪律，同「受限 vs 单笔校验被拒」）。
_Avoid_: 爽约、过期、被取消

**开始使用**：
预约转成存件订单的**唯一**动作，转单即生成开门指令。它是**不可逆的终态** —— 预约置 `USED` 后，即使关联订单被取消，预约也**不会回退**。
_Avoid_: 使用预约、激活、核销

### 信用与受限

**信用分**：
后端字段 `score`。扣减型的**行为约束**分 —— 订单超时、预约后未到店、违规占用会扣它，扣到低于平台规则下限，账号就会被限制。
它不是可兑换的消费积分，所以**不叫「积分」**。
_Avoid_: 积分、评分、分值

**受限**：
用户可见的**账号级**状态：账号处于限制中，不能开始新的动作（存件、创建预约、开始使用预约）。
**手上已有的订单不受影响，仍可正常取完。**
它是账号状态，与「某一笔动作没通过校验」是两回事。
_Avoid_: 封号、被拉黑、被禁止

**单笔校验被拒**：
某一笔动作因**该笔自身**的条件不成立而没通过（例如预约已过期、格口已满、柜机离线）。
它与「受限」是两回事 —— 尤其在中控屏上被拒绝时，用户极易误以为自己的账号出了事，两者在文案上必须能一眼分开。
_Avoid_: 业务拒绝（那是屏端的说法）、被限制
