# smart-locker-app

智能柜的**小程序 C 端**。它不拥有柜机，也不拥有账本 —— 柜机由中控屏那一侧驱动，订单与金额由后端定义，小程序只负责「把用户和柜机接上」。本文件只是术语表，实现口径见 `docs/spec/`。

## Language

### 柜机一侧

**柜机码**：
印在柜机上的编号（如 `WD-01`），是小程序找到某台柜机的唯一途径。用户扫的就是它。
_Avoid_: 设备码、柜机编号、二维码内容

**中控屏**：
柜机上那块公共触摸屏。用户在上面敲取件码、看订单摘要。
_Avoid_: 柜机屏、设备端、终端

**开门指令**：
服务端下发给柜机的开门命令，是**开门这件事的唯一驱动**。小程序开不了门，它只能请服务端生成一条指令。
_Avoid_: 开门请求、开箱命令

### 取件码

**取件码**：
6 位数字，同一柜机内唯一、历史不复用，用户**存入关门后**才生成。
它是**查询凭据，不是取件凭据** —— 在中控屏上敲入它能查出「哪一单、欠多少、付没付」，但它**打不开门**。
_Avoid_: 取件凭证、开箱码、取件密码

**取件开门**：
C 端唯一的开门动作。它把「清账」和「开门」合成同一个动作 —— 没清账就先清账，清过了就直接开门并重发指令。
_Avoid_: 取件、开箱、一键开门、支付（支付只是它未清账分支里的实现细节）

### 订单两段

**存入**：
订单的前半段 —— 下单开门、放东西、关门。**关门才是落定点**：关门事件之前订单没有计时起点，也就没有费用。
_Avoid_: 寄件、寄存

**取件**：
订单的后半段 —— 清账、开门、取走、关门结算。
_Avoid_: 领取、签收

### 信用与受限

**信用分**：
后端字段 `score`。扣减型的**行为约束**分 —— 订单超时、预约后未到店、违规占用会扣它，扣到低于平台规则下限，账号就会被限制。
它不是可兑换的消费积分，所以**不叫「积分」**。
_Avoid_: 积分、评分、分值

**受限**：
用户可见的**账号级**状态：账号处于限制中，不能开始新的动作（存件、创建预约、开始使用预约）。
**手上已有的订单不受影响，仍可正常取完。**
它是账号状态，与「某一笔动作没通过校验」是两回事。
_Avoid_: 封号、被拉黑、被禁止

**单笔校验被拒**：
某一笔动作因**该笔自身**的条件不成立而没通过（例如预约已过期、格口已满、柜机离线）。
它与「受限」是两回事 —— 尤其在中控屏上被拒绝时，用户极易误以为自己的账号出了事，两者在文案上必须能一眼分开。
_Avoid_: 业务拒绝（那是屏端的说法）、被限制
