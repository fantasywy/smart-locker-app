{
  "name": "miniprogram-ts-sass-quickstart",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "verify:tokens": "node scripts/verify-visual-tokens.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "",
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "miniprogram-api-typings": "^2.8.3-1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  }
}
