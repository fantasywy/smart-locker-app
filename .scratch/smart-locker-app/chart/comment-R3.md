**Findings 就位** → 分支 `research/r3-open-gaps`，文件 `docs/research/R3-open-gaps.md`（本地副本 `.scratch/smart-locker-app/research/R3-open-gaps.md`）。

**核心结论：题目预设被推翻 —— 三项「未闭合口径」在后端文档里其实都已闭合。**

| 编号 | 核实结果 | 判定 |
|---|---|---|
| **N1** `openedNotClosed` | 后端**已给字段名与类型**，正确拼写是 `openedNotClosed`（过去分词）：`API设计文档.md:1340`（#12）、`:493`、`:1363`、`CONTEXT.md:33-35`。管理端 fixlist 登记的「后端未给字段名」是**误登记**，且其暂定名 `openNotClosed` 拼写错。**唯一真实缺口**：该字段只挂在**管理端详情 §7.2**，**不在 C 端契约 §13.9 / §13.8** | C 端可自行裁定（保守方向） |
| **N2** `startAt` 未存入为空 | 后端多处明确：`需求分析文档.md:451/151/181/185`、`API设计文档.md:995/1213/1021`。管理端的非空 `string` 只是本地 TS 类型不精确 | C 端可自行裁定 |
| **N4** 未存入与使用中是否分开 | 后端**明示不拆分**：`需求分析文档.md:451`「今日订单拆分不区分未存入」、`API设计文档.md:1357`②；列表 API 只有 `status` 枚举、无独立筛选项 | C 端可自行裁定 |

**唯一要挂账的联调项**：把 `openedNotClosed` 加进 C 端 §13.9 —— 这属**后端契约改动**，在本 effort 的 **Out of scope** 内，**此处只登记、不提议**。在此之前 C 端按**保守方向（漏报而非误报）**处理：不渲染该异常标记。

**对 D3 的影响（本票最重要的下游）**

- 「未存入 / 使用中」的**唯一数据信号**是 `startAt === null`。派生式：`未存入 = status==='IN_PROGRESS' && !startAt`，`使用中 = status==='IN_PROGRESS' && !!startAt`。D3 可以放心用这个信号。
- 「异常」的表达空间被**收窄**：C 端无法可靠渲染「开门未闭」。D3 要么对该异常态留白，要么把「加字段」作为挂账项显式登记。

**未做**：不改后端、不改管理端；未改任何仓库。本票保持 open，待确认后关闭以解锁 D3。
